<?xml version="1.0" encoding="UTF-8"?>
<test-data
    xmlns="http://pmd.sourceforge.net/rule-tests"
    xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
    xsi:schemaLocation="http://pmd.sourceforge.net/rule-tests http://pmd.sourceforge.net/rule-tests_1_0_0.xsd">

    <test-code>
        <description>When getInstance() returns a protected key, then report a violation</description>
        <expected-problems>1</expected-problems>
        <expected-linenumbers>4</expected-linenumbers>
        <code><![CDATA[
public with sharing class ProtectedCustomSettingsHierarchyCtrl {
    @AuraEnabled
    public static Protected_API_Key_Hierarchy__c getInstanceUserId(Id userId){
        return Protected_API_Key_Hierarchy__c.getInstance(userId); //NEVER return secrets back to the end user
    }
}
        ]]></code>
    </test-code>
    <test-code>
        <description>When getInstance() does not return a protected key, then do not report a violation</description>
        <expected-problems>0</expected-problems>
        <code><![CDATA[
public with sharing class ProtectedCustomSettingsHierarchyCtrl {
    @AuraEnabled
    public static Some_Other_Class__c getInstanceUserId(Id userId){
        return SomeOtherClass.getInstance(userId);
    }
}
        ]]></code>
    </test-code>
</test-data>
