<?xml version="1.0" encoding="UTF-8"?>
<test-data
    xmlns="http://pmd.sourceforge.net/rule-tests"
    xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
    xsi:schemaLocation="http://pmd.sourceforge.net/rule-tests http://pmd.sourceforge.net/rule-tests_1_0_0.xsd">

    <test-code>
        <description>When credentials are hardcoded in variable assignment, then report a violation</description>
        <expected-problems>2</expected-problems>
        <expected-linenumbers>4,6</expected-linenumbers>
        <code><![CDATA[
public with sharing class HttpEndPointCall {
    public static void makeHttpCall(String userId) {
        String newPassword = '';
        newPassword = 'hardcoded password';
        setPassword(userId, newPassword);
        newPassword = newPassword + 'some other value';
        System.setPassword(userId, newPassword);
    }
}
        ]]></code>
     </test-code>
</test-data>
